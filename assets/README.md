# Local Cobalt assets

These small PNGs are shipped with Cobalt so the UI does not download files while
it starts. The loader tries this directory first and uses the built-in Roblox
fallbacks when a user runs only the single Luau bundle.
